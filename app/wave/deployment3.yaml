apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-hello-world3
  labels:
    app: nginx-hello-world3
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-hello-world2
  template:
    metadata:
      labels:
        app: nginx-hello-world2
    spec:
      containers:
        - name: nginx
          image: "harbor.cnsdomain.com/argocd/nginx:v0.0.1"
          ports:
            - containerPort: 8080
          lifecycle:
            postStart:
              exec:
                command: ["/bin/sh","-c","sleep 5"]